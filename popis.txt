Zábavná a kreativní hra na kreslení a hádání v reálném čase, kde hráči kombinují slova, aby vytvořili vtipná zadání, nakreslili je a ostatní hádali, o jakou frázi se jedná.

🎮 Jak hrát
Koncept Kreslíři je multiplayerová párty hra, která prověří vaše kreslířské dovednosti a vaši schopnost uhodnout, co se vaši přátelé snaží ztvárnit.

Spuštění hry
1. Zadejte své jméno: Zvolte si přezdívku pro hru.
2. Vytvořit hru: Jako hostitel můžete vytvořit novou herní místnost. Obdržíte unikátní ID hry, které můžete sdílet s přáteli.
3. Připojit se ke hře: Zadejte ID hry poskytnuté hostitelem a připojte se do existující herní místnosti.

Herní místnost (Lobby)
Jakmile jste v lobby, uvidíte všechny připojené hráče. Hostitel může spustit hru, jakmile jsou v místnosti alespoň dva hráči.

Výběr slov
Na začátku každého kola je jeden hráč vybrán jako Umělec. Umělci jsou předloženy tři kategorie slov: Vlastnost, Subjekt a Činnost. Umělec musí z každé kategorie vybrat jedno slovo, aby vytvořil jedinečnou, často komickou frázi (např. "Vzteklá velryba hrající golf"). Ostatní hráči uvidí pouze to, že umělec vybírá slova, ale ne slova samotná.

Kreslení a hádání
Jakmile je fráze vybrána, začíná fáze kreslení! Umělec vidí celou frázi a musí ji nakreslit na plátno. Spustí se odpočet času. Ostatní hráči (hádající) vidí prázdné plátno a frázi s podtržítky místo slov (např. _______   _______   ___________). Jak umělec kreslí, obrázek se v reálném čase zobrazuje všem hádajícím. Hádající píší své tipy do chatovacího okna.

Bodování
Když hádající správně napíše jedno ze slov z fráze, toto slovo se odhalí všem a hádající získá body. Čím rychleji hádáte, tím více bodů získáte! Umělec také získává body za každé slovo, které ostatní správně uhodnou. Kolo končí, když vyprší časový limit, nebo když jsou všechna slova z fráze uhodnuta.

Vítězství ve hře
Hra pokračuje po stanovený počet kol, aby se každý dostal na řadu jako umělec. Na konci posledního kola je hráč s nejvyšším skóre korunován vítězem!

✨ Funkce
- Multiplayer v reálném čase: okamžité aktualizace kreslení a tipů přes WebSockety.
- Dynamický obsah: balíčky slov (různá témata) generují pokaždé jiné fráze.
- Interaktivní kreslicí plátno: barvy, velikosti štětce, guma a mazání plátna.
- Živý přehled tipů: správné odpovědi se odhalují automaticky.
- Animovaná tabulka skóre: živě aktualizované pořadí hráčů.
- Responzivní design: hra funguje na PC i na mobilu.

Hraní mezi PC a mobilem (rychlé instrukce)
- Spusťte backend na hostitelském počítači s povoleným přístupem z lokální sítě: `uvicorn main:app --host 0.0.0.0 --port 8000`.
- Spusťte frontend v režimu dostupném v síti: `npm run dev -- --host` v adresáři frontend.
- Otevřete veškerá zařízení ve stejné Wi‑Fi síti na adrese `http://<IP_HOSTA>:5173` (PC i mobil). Jeden hráč vytvoří hru, ostatní se připojí pomocí kódu.

Poznámky k mobilnímu UX
- Mobilní verze nabízí drawer pro chat a skóre a možnost rozbalit plátno na celou obrazovku.
- Doporučené: používejte moderní mobilní prohlížeč (Chrome, Safari) a povolit otáčení obrazovky pro lepší zážitek.

🛠️ Použité technologie
Frontend: React, TypeScript
Stylování: Tailwind CSS
Komunikace v reálném čase: WebSockets

Upozornění: Pro připojení mezi zařízeními musí být všechna zařízení ve stejné lokální síti nebo musí být backend dostupný přes veřejnou adresu (tunel jako ngrok nebo nasazený server).

#!/usr/bin/env bash
# start-dev.sh - spustí backend a frontend pro lokální vývoj

set -euo pipefail
ROOT_DIR=$(cd "$(dirname "$0")/.." && pwd)
cd "$ROOT_DIR"

# Spustíme backend v subshellu
echo "Spouštím backend..."
cd backend
uvicorn main:app --host 0.0.0.0 --port 8000 --reload &
BACKEND_PID=$!
cd "$ROOT_DIR"

# Spustíme frontend
echo "Spouštím frontend..."
cd frontend
if [ ! -d "node_modules" ]; then
  npm install --silent
fi
npm run dev -- --host &
FRONTEND_PID=$!
cd "$ROOT_DIR"

# Trap pro ukončení obou procesů
trap "echo 'Zastavuji služby...'; kill $BACKEND_PID $FRONTEND_PID 2>/dev/null || true; exit 0" SIGINT SIGTERM

# Čekáme na oba procesy
wait $BACKEND_PID
wait $FRONTEND_PID

# start-dev.ps1 - spustí backend a frontend pro lokální vývoj (PowerShell)
Param()
$scriptRoot = Split-Path -Parent $MyInvocation.MyCommand.Definition
Set-Location $scriptRoot\..

Write-Host "Spouštím backend..."
Set-Location .\backend
if (Test-Path .\venv\Scripts\Activate.ps1) {
    Write-Host "Aktivuji virtuální prostředí"
    . .\venv\Scripts\Activate.ps1
}
$backend = Start-Process -FilePath "uvicorn" -ArgumentList "main:app --host 0.0.0.0 --port 8000 --reload" -PassThru
Set-Location ..\frontend
Write-Host "Spouštím frontend..."
if (-Not (Test-Path .\node_modules)) {
    npm install
}
$npm = Start-Process -FilePath "npm" -ArgumentList "run dev -- --host" -PassThru
Set-Location $scriptRoot\..

Write-Host "Backend PID: $($backend.Id), Frontend PID: $($npm.Id)"

try {
    while ($true) { Start-Sleep -Seconds 1 }
} finally {
    Write-Host "Zastavuji procesy..."
    if ($backend -ne $null) { $backend.Kill() }
    if ($npm -ne $null) { $npm.Kill() }
}